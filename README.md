# Simple CD realization via SSH with Python script 
Before usage install Paramiko library:
```
pip install paramiko
```
Run:
```
python deploy.py "<username>" "<server_ip>" "<password>" "<source_folder>" "<destination_folder>" 
```
